## Gemini (client-side)
#
# NOTE: This value is bundled into the frontend at build time, so it is NOT a secret.
# For a real production deployment, proxy Gemini calls through a backend.
VITE_GEMINI_API_KEY=your_key_here

